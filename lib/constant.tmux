#!/usr/bin/env bash
# lib/constants.tmux

# Powerline Separators
export SEP_SOLID_LEFT=""  # Used to transition into a new background color
export SEP_THIN_LEFT=""   # Used to separate modules sharing the same background

# Default module lists if the user doesn't define any
export DEFAULT_MODULES_RIGHT="date time hostname"
